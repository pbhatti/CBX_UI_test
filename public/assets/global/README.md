# Global assets

Shared across the app: logos, common icons (search, settings, notifications, etc.).

Use fixed icon containers per `.cursor/skills/icons` (icons skill). Path in code: `/assets/global/<filename>`.
